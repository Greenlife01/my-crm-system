# Server
PORT=3000

# Follow-up email delay (days after lead creation)
FOLLOWUP_DAYS=3

# SMTP settings (optional — if not set, Ethereal test emails are used)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your@email.com
# SMTP_PASS=your-password
# SMTP_FROM="CRM System" <your@email.com>
